# internal/domain/

Logique métier pure. **Aucune dépendance externe** (pas d'imports vers `adapters/`, pas de drivers, pas de framework).

À ce stade (Lot 0), ce dossier est volontairement vide : le backend ne fait que servir `/health`. Les entités et règles métier apparaîtront avec les lots ultérieurs (progression, sessions, SRS côté serveur si pertinent).

Voir [`docs/rfc/0003-architecture-hexagonale.md`](../../../docs/rfc/0003-architecture-hexagonale.md).
