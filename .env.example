# Copier en `.env` (non versionné) avant `docker compose up -d`.
# Jeton d'accès à l'API de synchronisation (cf. RFC 0014).
# Générer un secret aléatoire, par ex. : openssl rand -base64 32
# Laisser vide = API ouverte (réseau de confiance / LAN uniquement).
SINO_API_TOKEN=
